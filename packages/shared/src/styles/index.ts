/**
 * Shared styles - className strings used by both web and mobile.
 *
 * @example
 * ```typescript
 * import { trendBadgeStyles } from "@somar/shared/styles";
 * import { quickActionStyles } from "@somar/shared/styles";
 * ```
 */

export * from "./components";
